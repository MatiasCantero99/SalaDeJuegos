import { JuegoFiltroPipe } from './juego-filtro.pipe';

describe('JuegoFiltroPipe', () => {
  it('create an instance', () => {
    const pipe = new JuegoFiltroPipe();
    expect(pipe).toBeTruthy();
  });
});
